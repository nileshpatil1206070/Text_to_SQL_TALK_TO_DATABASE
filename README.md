# Text_to_SQL_TALK_TO_DATABASE
Convert natural language queries into SQL using Ollama and execute them on a SQLite database through a Streamlit application. Used open source model qwen for generating the query.
